# =============================================================================
# Dev mode overrides — used when running: mvn compile quarkus:dev
# =============================================================================

# Override colour for local dev so it's obvious it's not OCP
app.colour=local-dev
app.version=1.0.0-dev

# Swagger UI always on
quarkus.swagger-ui.always-include=true
